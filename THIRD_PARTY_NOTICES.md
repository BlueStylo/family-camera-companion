# Third-Party Notices

The lockfile pins these direct dependencies:

| Component | License | Use |
| --- | --- | --- |
| Three.js | MIT | WebGL rendering and GLTFLoader |
| lucide | ISC | UI icons |
| esbuild | MIT | Browser bundling |

Upstream notices are retained in the generated bundle where supplied; package
licenses are included by npm in the dependency installation.

Blender is an external authoring tool, not bundled in this repository.
FFmpeg is an optional external camera/media process; its license depends on
the installed build. No FFmpeg binary, camera firmware or proprietary vendor
SDK is redistributed here. Generated example media in tests is temporary.

The two house textures are generated by the model-building script. Original
photographs and third-party map/road-view imagery are not included.
